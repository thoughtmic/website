/**
 * Nav logo animation is now CSS-only (continuous pulse via
 * brand-mark-pulse keyframe in components.css). No JS needed.
 *
 * This file is kept as a no-op so existing <script defer
 * src="/js/nav-mark.js"></script> tags across the site continue
 * to load without 404 errors. Can be deleted in a follow-up
 * cleanup along with the script tags themselves.
 */
